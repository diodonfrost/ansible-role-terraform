---
# vars file for terraform in SunOS system
_terraform_pkg_url: "https://releases.hashicorp.com/terraform/{{ _terraform_version }}/terraform_{{ _terraform_version }}_solaris_amd64.zip"
_terraform_default_path: /usr/sbin
